const expect = require('chai').expect;
// const request = require('supertest');

const app = require('../routes/index');

describe( 'Check Post Request', () => {
    // before((done) => {

    // });

    // after((done) => {

    // });

    it('App work properly', () => {
        expect(true).to.equal(true);
    })
})